% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animation_controls.R
\name{remove_timeline_control}
\alias{remove_timeline_control}
\title{Remove the timeline control from the map.}
\usage{
remove_timeline_control(proxy, panel_id = NULL)
}
\arguments{
\item{proxy}{The map proxy object created by \code{mapProxy()}.}

\item{panel_id}{Optional. If provided, removes the timeline control from the specified control panel.
If NULL, removes the standalone timeline control.}
}
\value{
\if{html}{\out{<div class="sourceCode">}}\preformatted{     The map proxy object for chaining.
}\if{html}{\out{</div>}}
}
\description{
Remove the timeline control from the map.
}
\examples{
if (interactive()) {
# Add to a map
map() |>
 add_timeline_control()
# In an observer
mapProxy("map") |>
 remove_timeline_control()

# Add to a control panel
map() |>
 add_control_panel(panel_id = "my_panel", title = "Map Settings") |>
 add_timeline_control(panel_id = "my_panel")
# In an observer
mapProxy("map") |>
 remove_timeline_control(panel_id = "my_panel")
}
}
